# --- LLM (OpenAI-compatible) ---
OPENAI_API_KEY="your_key_here"
OPENAI_BASE_URL=""        # optional, e.g. https://api.openai.com/v1 or your proxy
OPENAI_MODEL="gpt-4o-mini" # choose what you have available
OPENAI_TEMPERATURE="0.2"

# If OPENAI_API_KEY is empty, the server runs in MOCK mode (no external calls).
